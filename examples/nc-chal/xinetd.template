service chal
{
    disable = no
    type = UNLISTED
    socket_type = stream
    protocol = tcp
    server = /home/chal/run.sh
    user = chal
    port = __CHAL_PORT__
    flags = REUSE
    bind = 0.0.0.0
    wait = no
}
